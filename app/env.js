/*
 * env.js
 * These are app environment constants such as where our backend is located, what
 * version of the API we are using, etc
 */

const API_URL = 'http://coding-challenge.osedea.com/api/v1/';

export default API_URL;
